env: local

server:
  port: 8080
  read_timeout: 10s
  write_timeout: 10s
  idle_timeout: 60s

database:
  max_open_conns: 25
  max_idle_conns: 10
  conn_max_lifetime: 5m

log:
  level: debug
  format: json
